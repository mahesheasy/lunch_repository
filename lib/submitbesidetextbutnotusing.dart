// import 'package:flutter/material.dart';

// List submitbesidetextitems = ['Lock Cheyyala ? ', 'Lock Karna Kya ?'];
// List<String> _list = ['Day 1', 'Day 2', 'Day 3'];
// int _currentIndex = 0;

// Text submitbesidetext(int submitbesidetextitemsIndex, BuildContext context) {
//   return;

//   Text(submitbesidetextitems[submitbesidetextitemsIndex],
//       style: Theme.of(context).textTheme.displayLarge);
// }

// @override
// void initState() {
//   super.initState();

//   // Create a Timer to update the list every 24 hours
//   Timer.periodic(Duration(hours: 24), (timer) {
//     setState(() {
//       _currentIndex = (timer.tick + 1) %
//           _list.length; // Update the current index of the list
//     });
//   });
// }
